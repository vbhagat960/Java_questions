package GFG;

public class Reverse {
	public static void main(String[] args) {
		String str = "geeksforgeeks",ch = "";
		for(int i=0;i<str.length();i++) {
			ch = str.charAt(i)+ch;
		}
		System.out.println(ch);
	}
}
